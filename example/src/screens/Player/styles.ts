import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 24,
  },
  control: {
    marginLeft: 16,
  },
  header: {
    flexDirection: 'row',
    marginBottom: 16,
  },
});
